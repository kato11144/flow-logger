🚀 Commands
```
# 仮想環境を作成
python3 -m venv .venv

# 仮想環境を有効化
. .venv/bin/activate

# 必要なパッケージをインストール
pip3 install -r requirements.txt

# 仮想環境を無効化
deactivate
```
